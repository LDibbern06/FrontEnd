frutas.map = numeros.map(x );
console.log(frutas);

const prompt =require("prompt-sync")();

let frutas = []; 

function (){
    let nota = Fruta(prompt("Escolha uma fruta: "));
    notas.push(fruta);
}
